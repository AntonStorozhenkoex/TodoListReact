import React from "react";
import '../../index.css';

const Button = () => {
    return (
      <button className="addButton" type="submit">
        Add
      </button>
    );
}
export default Button;